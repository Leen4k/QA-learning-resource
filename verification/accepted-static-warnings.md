# Accepted static-check warnings

Baseline recorded 2026-08-29. These five lessons predate the `<dfn>` grounding
convention. Their terms are already present in `GROUNDED.md`, so the warnings are
accepted until those lessons are deliberately revised:

- lessons/01-quality-risk-triage.html :: no `<dfn>` terms — new lessons should mark the terms they ground (see GROUNDED.md); pre-convention lessons can ignore this
- lessons/02-test-levels-and-types.html :: no `<dfn>` terms — new lessons should mark the terms they ground (see GROUNDED.md); pre-convention lessons can ignore this
- lessons/03-static-testing.html :: no `<dfn>` terms — new lessons should mark the terms they ground (see GROUNDED.md); pre-convention lessons can ignore this
- lessons/05-equivalence-and-boundaries.html :: no `<dfn>` terms — new lessons should mark the terms they ground (see GROUNDED.md); pre-convention lessons can ignore this
- lessons/08-one-page-test-plan.html :: no `<dfn>` terms — new lessons should mark the terms they ground (see GROUNDED.md); pre-convention lessons can ignore this

No other full-course warning is accepted. A new or changed warning must be
investigated, fixed, or explicitly approved before this baseline changes.
